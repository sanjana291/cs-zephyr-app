# calixto-ethernet driver module
# Add your driver subdirectory below when ready, e.g.:
#
# zephyr_include_directories(${CMAKE_CURRENT_SOURCE_DIR}/include)
# add_subdirectory_ifdef(CONFIG_CALIXTO_ETH drivers/ethernet/<chip>)
